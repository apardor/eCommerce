import express, {Router} from 'express';

const router = Router();


router.get('/user', () => {})
router.get('/user/:id', () => {})
router.patch('/user/:id', () => {})
router.post('/user', () => {})
router.delete('/user/:id', () => {})
